#include<stdio.h>
int main()
{
    /*int i = 0; //loop;
    //scanf("%d", &loop);

    while(i<3)
    {
        int a,b,sum;
        scanf("%d", &a);
        scanf("%d", &b);

        sum=a+b;
        printf("%d\n", sum);

        i++;
    }*/
    int d;
    scanf("%d", &d);
    int i = 0; //loop;
    //scanf("%d", &loop);

    while(i<d)
    {
        int a,b,sum;
        scanf("%d", &a);
        scanf("%d", &b);

        sum=a+b;
        printf("%d\n", sum);

        i++;
    }
    return 0;
}
