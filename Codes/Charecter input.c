 #include<stdio.h>
int main(){


    char d[100];
    char tmp[100];
       gets(d);

    //scanf("%c", &d[0]);
    //d[0] = getchar();
   // d[1] = getchar();
   // gets(tmp);
    //printf("%c\n", d[0]);

    //scanf("%s", &d); (Input ends by pressing space)
    //dgets(d); //(Input ends by pressing enter)
    //printf("%s\n", d);
   // char a[7];
    //char tmp[7];
    //gets(a);
   //gets(tmp);
    return 0;

}

